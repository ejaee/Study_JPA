
spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create # 애플리케이션 실행 시점에 테이블을 다시 생성한다.
#    properties:
#      hibernate:
#        # show_sql: true // System.out 으로 찍는다
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug # hibernate 가 생성하는 모든 SQL 로그가 디버그 모드로 다 찍힌다.
  org.hibernate.type: trace